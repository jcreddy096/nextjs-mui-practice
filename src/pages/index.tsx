import React from 'react';
import { Box } from '@mui/material';
import  Board  from '../components/Board';

const Home = () => {
  return (
    <Box sx={{ padding: '20px' }}>
      <Board />
    </Box>
  );
};

export default Home;
